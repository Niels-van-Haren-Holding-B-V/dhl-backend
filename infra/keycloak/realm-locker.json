{
  "realm": "locker",
  "enabled": true,
  "registrationAllowed": false,
  "sslRequired": "none",
  "clients": [
    {
      "clientId": "dhl-backend",
      "name": "DHL backend (client credentials)",
      "enabled": true,
      "publicClient": false,
      "serviceAccountsEnabled": true,
      "standardFlowEnabled": false,
      "directAccessGrantsEnabled": false,
      "secret": "${LOCKER_CLIENT_SECRET}"
    }
  ]
}
